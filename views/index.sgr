extends(src='layout.sgr')
  //-block(name='banner')
    //-h1.banner Software Engineer & Consultant
  block(name='content')
    p(md) Example of **markdown** with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content 

    p(md) Example of **markdown** with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content  with lots of content 
